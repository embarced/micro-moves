{
  "name": "rules",
  "version": "0.1.0",
  "description": "rules for the game of chess",
  "license": "Apache-2.0",
  "main": "server-main.js",
  "scripts": {
    "start": "node server-main.js",
    "test": "npx mocha tests --recursive",
    "test-jenkins": "JUNIT_REPORT_PATH=./testreports/tests.xml npx mocha tests --recursive --reporter mocha-jenkins-reporter",
    "dev": "npx nodemon server-main.js"
  },
  "author": "StefanZ",
  "dependencies": {
    "mocha-jenkins-reporter": "0.4.5",
    "express": "^4.16.2"
  }
}
